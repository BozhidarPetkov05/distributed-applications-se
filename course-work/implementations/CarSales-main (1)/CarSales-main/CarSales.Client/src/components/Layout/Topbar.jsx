import React from 'react';

const TopBar = () => {
    return (
        <header className="top-bar">
            <div className="brand">Car Sales</div>
        </header>
    );
};

export default TopBar;